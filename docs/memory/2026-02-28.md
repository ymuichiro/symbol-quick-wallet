# 2026-02-28

## Entry 1
- Time: 2026-02-28
- Decision/Intent: Standardize live test key handling to file-based flow and remove hardcoded key dependency.
- Action:
  - unified key resolution in `tests/conftest.py` (`--test-key-file` first, env fallback)
  - added `--require-live-key`
  - removed `tests/live_test_key.py` and `scripts/set_live_test_key.py`
  - updated test docs and `.gitignore`
- Result: live tests now rely on shared fixture path and support strict fail mode when keys are missing.
- Next: validate funded test account balance and run strict live suites when requested.

## Entry 2
- Time: 2026-02-28
- Decision/Intent: Verify whether funded test account balance reflects incoming transfer.
- Action:
  - derived address from `/tmp/symbol-quick-wallet-test-key`
  - queried testnet account endpoint (`/accounts/{address}`)
- Result:
  - address: `TBA6CIVYPYKTLAI6MBBGGOXRXA7W2HZGNDBQ6BY`
  - mosaic `72C0212E67A08BCE` amount `1000000000` micro XYM
  - computed balance: `1000 XYM`
- Next: proceed to strict live feature tests with this key when instructed.

## Entry 3
- Time: 2026-02-28
- Decision/Intent: Stabilize strict live `integration and slow` by fixing real implementation gaps first, then handling node capability limits explicitly.
- Action:
  - fixed wallet account response parsing (`/accounts/{address}` wrapper) and mosaic normalization (`id`/`mosaicId`)
  - added backward-compatible `TransactionManager.wait_for_transaction_status` alias
  - changed mosaic flow to two-step definition + supply change with fee assignment
  - normalized mosaic REST path IDs to 16-hex format (no `0x`)
- Result:
  - live mosaic tests now pass end-to-end against funded testnet account
  - transfer live transaction tests relying on balance/status checks pass
- Next: classify aggregate slow failures as implementation issues vs node restrictions.

## Entry 4
- Time: 2026-02-28
- Decision/Intent: Avoid false negatives in aggregate live tests when current node/network prohibits or does not expose aggregate state.
- Action:
  - added aggregate prohibited-code detection in slow integration tests
  - added bounded waits + explicit skip when bonded aggregate status/partial visibility is unavailable on node
  - updated AGENTS.md with latest verified status, live key workflow, and node capability notes
- Result:
  - strict runs completed successfully:
    - `integration and not slow`: `72 passed, 19 skipped`
    - `integration and slow`: `16 passed, 5 skipped`
    - `unit`: `107 passed`
- Next: if full bonded workflow validation is required, test against a node/network where aggregate bonded is fully enabled and observable.

## Entry 5
- Time: 2026-02-28
- Decision/Intent: Increase non-slow integration coverage by re-enabling mosaic transaction-creation tests that were historically skipped.
- Action:
  - removed obsolete skip decorators from `TestMosaicTransactionCreation`
  - updated assertions for SDK `MosaicFlags` enum (`flags.value`)
  - reran strict non-slow integration suite
- Result:
  - mosaic transaction-creation integration tests pass (`4 passed`)
  - updated non-slow strict integration result: `76 passed, 15 skipped`
- Next: keep aggregate bonded deep verification as a separate track tied to a fully aggregate-enabled node.
## 2026-02-28 Daily bug scan (automation: daily-bug-scan)
- Scope: scanned commits from last 24h (last run: never).
- Evidence reviewed: `b8e54ecece183bbd9cd33e790730416d0df675ec`, `05763cd6c10fb9522cb41a7cde55c2004147173f`.
- Finding: release commit bumped `pyproject.toml` to `0.6.2` but runtime version in `src/__init__.py` remained stale.
- Fix applied: updated `src/__init__.py` `__version__` from `0.6.0` to `0.6.2`.
- Validation: shell check confirms `pyproject` and `__version__` now match (`0.6.2`).
- Notes: could not run `uv` commands in sandbox due runtime panic in `uv` binary.
