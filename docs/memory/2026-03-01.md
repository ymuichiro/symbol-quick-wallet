# 2026-03-01

## Entry 1
- Time: 2026-03-01
- Decision/Intent: Re-run strict live suites after additional testnet funding to validate previous mosaic insufficient-balance failures.
- Action:
  - confirmed funded account balance via `/tmp/symbol-quick-wallet-test-key` wallet load
  - reran `tests/features/mosaic/test_integration.py -m 'integration and slow'`
  - reran full `integration and slow` and `integration and not slow` with strict key requirements
- Result:
  - address: `TBA6CIVYPYKTLAI6MBBGGOXRXA7W2HZGNDBQ6BY`
  - balance: `3,028.8528 XYM`
  - mosaic slow suite: `3 passed`
  - strict `integration and slow`: `16 passed, 5 skipped`
  - strict `integration and not slow`: `76 passed, 15 skipped`
- Next: complete remaining skip items tied to aggregate/node capability and namespace ID verification.

## Entry 2
- Time: 2026-03-01
- Decision/Intent: Clarify what still blocks “all features fully verified against real nodes”.
- Action:
  - captured `-rs` skip summaries for both strict integration markers
- Result:
  - aggregate create/sign descriptor tests still skipped due to SDK compatibility gates
  - aggregate live complete/bonded behavior partially skipped on current node (`Failure_Aggregate_V1_Prohibited` / bonded visibility unavailable)
  - two namespace resolution tests still skipped pending namespace ID algorithm verification
- Next: use an aggregate-capable testnet node and finalize namespace ID algorithm verification tests.

## Entry 3
- Time: 2026-03-01
- Decision/Intent: Validate user claim that aggregate latest is v3 and remove legacy v1/v2 implementation usage.
- Action:
  - switched aggregate transaction descriptors to v3:
    - `aggregate_complete_transaction_v3`
    - `aggregate_bonded_transaction_v3`
  - updated multisig aggregate complete creation to v3
  - removed obsolete aggregate integration skip decorators tied to old SDK limitation note
  - verified live announce behavior against testnet node
- Result:
  - v1/v2 announce returns prohibited, v3 announces and confirms successfully on testnet
  - aggregate bonded flow works end-to-end, though partial visibility can still be node-dependent
- Next: keep bonded tests tolerant to `confirmed` vs `partial` observability differences.

## Entry 4
- Time: 2026-03-01
- Decision/Intent: Stabilize strict live suite against transient node DNS/connectivity errors without hiding real implementation regressions.
- Action:
  - added reachable-node selection in `tests/conftest.py`:
    1. `SYMBOL_TEST_NODE_URL`
    2. `SYMBOL_TEST_NODE_URLS`
    3. defaults (`sym-test-01`, `sym-test-03`)
  - wired live aggregate integration tests to use `wallet.node_url` instead of fixed constant
  - fixed aggregate slow service test using invalid checksum address by switching to known valid self address
  - added configurable live confirmation timeout (`SYMBOL_TEST_CONFIRM_TIMEOUT`, default 300s) in transfer/mosaic live tests
  - extended mosaic announce helper to accept confirmation/registration timeout parameters
- Result:
  - strict suites after fix:
    - `integration and not slow`: `89 passed, 2 skipped`
    - `integration and slow`: `19 passed, 2 skipped`
  - remaining 2 skips are aggregate bonded observability limitations on current node
- Next: if full no-skip aggregate bonded verification is required, run against a node with stable partial visibility guarantees.

## Entry 5
- Time: 2026-03-01
- Decision/Intent: Re-verify final stability after bonded-confirmed hash fallback enhancement in aggregate live tests.
- Action:
  - added confirmed-by-hash fallback check via `/transactions/confirmed/{hash}` before skipping bonded visibility tests
  - reran aggregate slow file and full strict suites:
    - `tests/features/aggregate/test_integration.py -m 'integration and slow'`
    - full `integration and slow`
    - full `integration and not slow`
- Result:
  - aggregate slow file: `2 passed, 2 skipped`
  - strict `integration and slow`: `19 passed, 2 skipped`
  - strict `integration and not slow`: `89 passed, 2 skipped`
  - skip profile unchanged: bonded visibility unavailable on current node, namespace ID verification pending
- Next: verify namespace ID algorithm against SDK and remove remaining namespace skips.

## Entry 6
- Time: 2026-03-01
- Decision/Intent: Remove namespace resolution skips by replacing custom namespace ID algorithm with Symbol SDK `IdGenerator`.
- Action:
  - updated namespace ID/path generation in:
    - `src/features/namespace/service.py`
    - `src/wallet.py`
  - enabled previously skipped namespace resolution tests and corrected expected alias mosaic ID for testnet (`72C0212E67A08BCE`)
  - reran namespace service test file and full strict non-slow integration suite
- Result:
  - `tests/features/namespace/test_service.py`: `20 passed`
  - strict `integration and not slow`: `91 passed, 409 deselected` (no skips)
  - remaining strict skip only in aggregate bonded slow tests (`2 skipped`)
- Next: to reach zero-skip strict mode, provide a node where aggregate bonded partial/status visibility is guaranteed.

## Entry 7
- Time: 2026-03-01
- Decision/Intent: Final regression verification after namespace ID algorithm migration.
- Action:
  - reran strict slow integration suite after wallet/namespace service updates
  - reran static checks for runtime code
- Result:
  - strict `integration and slow`: `19 passed, 2 skipped, 479 deselected`
  - `uv run ruff check .`: pass
  - `uv run ty check src`: pass
  - unresolved area remains unchanged: 2 aggregate bonded visibility skips on current node
- Next: if user provides/approves an alternate node with stable partial visibility, attempt zero-skip slow run.
