# 2026-03-04

## Entry 1
- Time: 2026-03-04
- Decision/Intent: Audit current implementation status for real-node testnet connectivity coverage across features before proceeding to UI polish.
- Action:
  - inspected local branch/worktree status and touched files
  - mapped feature test files and integration/slow markers
  - reviewed integration sections for lock/metadata/multisig/monitoring to verify whether tests actually hit real nodes or rely on mocks
  - executed validation commands:
    - `uv run pytest -q -m "integration and not slow" -rs`
    - `uv run pytest -q -m "integration and slow" -rs`
    - `uv run pytest -q -rs`
    - `uv run pytest -q --cov=src --cov-report=term-missing:skip-covered`
    - `uv run ruff check src/`
    - `uv run ty check src/`
    - `uv run ty check`
- Result:
  - current local full suite status: `445 passed, 55 skipped`
  - integration (not slow): `56 passed, 35 skipped`
  - integration (slow): `1 passed, 20 skipped`
  - all integration slow skips in this run were due to missing live key (`No test private key available`), so live on-chain announce flows were not validated in this audit run
  - static checks: `ruff check src` and `ty check src` passed; full `ty check` failed with 34 diagnostics in tests (known test typing assertions)
  - coverage snapshot: `TOTAL 33%`; significant uncovered areas remain in handler/screen layers and some service paths (notably metadata/transaction monitoring/transaction manager codepaths)
  - identified gaps for real-node validation:
    - metadata feature currently has only unit tests (no `@pytest.mark.integration` in `tests/features/metadata/test_service.py`)
    - lock integration tests are marked integration but announce path is mocked in `tests/features/lock/test_service.py`
    - monitoring "integration" tests currently validate start/stop and address normalization only, without confirmed ws handshake/message assertions
    - multisig integration includes weak tautological assertions (`result is not None or result is None`) and slow tests mostly validate tx construction/signing without announce confirmation
- Next:
  - run strict live suites with key (`--test-key-file ... --require-live-key`) to remove skip ambiguity
  - add/strengthen live integration tests for metadata, lock, monitoring, and multisig announce-confirm flows
  - only after above completion, move to UI layout/design adjustment tasks

## Entry 2
- Time: 2026-03-04
- Decision/Intent: Complete all previously listed unfinished tasks (real-node coverage gaps, live-path validation, typing debt) and reach commit-ready state.
- Action:
  - added/expanded integration tests for uncovered real-node paths:
    - metadata: read integration + slow live assign/remove flow
    - lock: real-node read integration + slow live secret lock/proof + hash lock announce
    - multisig: strengthened integration assertions + slow live initiate/confirm flow
    - monitoring: real WebSocket connect/subscribe/block-notification integration coverage
  - fixed monitoring WebSocket compatibility issue by adding ws->wss automatic upgrade on HTTPS-port handshake errors
  - fixed metadata transaction path compatibility:
    - switched transaction manager aggregate complete construction to v3 dictionary descriptor style
    - replaced unsupported embedded metadata descriptor calls with `transaction_factory.create_embedded(...)`
  - fixed secret proof transaction serialization bug in lock service (`proof` now sent as raw bytes instead of hex text bytes)
  - resolved full `ty check` diagnostics by tightening nullable assertions in tests
- Result:
  - static checks:
    - `uv run ruff check .` -> pass
    - `uv run ty check` -> pass
  - strict live integrations (with `/tmp/symbol-quick-wallet-test-key` and `--require-live-key`):
    - `integration and not slow`: `94 passed, 421 deselected`
    - `integration and slow`: `24 passed, 2 skipped, 489 deselected`
    - remaining 2 skips are aggregate bonded visibility limitations on current node (existing known limitation)
  - full suite with strict live key + live flag:
    - `SYMBOL_TEST_RUN_LIVE=1 uv run pytest -q tests --test-key-file /tmp/symbol-quick-wallet-test-key --require-live-key -rs`
    - result: `513 passed, 2 skipped`
- Next:
  - commit completed implementation set
  - proceed to UI layout/design adjustment workstream
