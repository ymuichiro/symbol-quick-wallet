# 2026-03-05

## Status Update

### Scope
- Requested item `1`: backend logic must be validated through real interactions with blockchain nodes and succeed.
- Requested item `3`: UI must be enterprise-grade and visually polished.

## Item 1 (Real-node Validation) - Completed

- [x] Eliminate aggregate bonded observability skip fallback and enforce deterministic multi-node visibility assertions.
  - Updated `tests/features/aggregate/test_integration.py` to:
    - add observer-node status/confirmed/partial lookup helpers
    - replace bonded "status unavailable" skips with assertions based on observer-node visibility
    - retain explicit skip only for node/network aggregate-prohibited policy status

- [x] Add multi-node verification evidence for aggregate flows.
  - Added integration tests:
    - `TestAggregateObserverVisibility::test_recent_confirmed_aggregate_is_visible_on_observer_nodes`
    - validates `aggregate_complete` and `aggregate_bonded` confirmed hashes across at least two observer nodes
  - Execution evidence (2026-03-05, JST):
    - `SYMBOL_TEST_NODE_URL=http://sym-test-01.opening-line.jp:3000 uv run pytest -q tests/features/aggregate/test_integration.py -m "integration and not slow" --test-key-file /tmp/symbol-quick-wallet-test-key --require-live-key -rs`
      - `19 passed, 4 deselected`
    - `SYMBOL_TEST_NODE_URL=http://sym-test-03.opening-line.jp:3000 uv run pytest -q tests/features/aggregate/test_integration.py -m "integration and not slow" --test-key-file /tmp/symbol-quick-wallet-test-key --require-live-key -rs`
      - `19 passed, 4 deselected`
    - `SYMBOL_TEST_RUN_LIVE=1 SYMBOL_TEST_NODE_URL=http://sym-test-01.opening-line.jp:3000 uv run pytest -q tests/features/aggregate/test_integration.py -m "integration and slow" --test-key-file /tmp/symbol-quick-wallet-test-key --require-live-key -rs`
      - `4 skipped, 19 deselected` (all skips are insufficient balance on generated key account; no bonded-visibility skip path)
    - `SYMBOL_TEST_RUN_LIVE=1 SYMBOL_TEST_NODE_URL=http://sym-test-03.opening-line.jp:3000 uv run pytest -q tests/features/aggregate/test_integration.py -m "integration and slow" --test-key-file /tmp/symbol-quick-wallet-test-key --require-live-key -rs`
      - `4 skipped, 19 deselected` (same reason)

- [x] Document final real-node validation matrix per feature.
  - Added `Feature Validation Matrix (Real Node)` to `tests/README.md` for:
    - account, transfer, aggregate, mosaic, namespace, multisig, lock, metadata, monitoring
  - Includes read integration, live write integration, and known strict-live skip conditions.

## Item 3 Remaining Tasks (Enterprise UI Polish)

- [ ] Perform UI audit across all main screens in terminal app.
  - Completion criteria:
    - Capture a screen-by-screen issue list (layout breakpoints, visual hierarchy, spacing, readability, status/error visibility).
    - Prioritize issues with severity (`high/medium/low`) and affected screens.
- [ ] Define and apply unified design tokens for enterprise readability.
  - Completion criteria:
    - Standardize typography scale, spacing scale, panel density, color semantics (success/warn/error/info), and focus/selection states.
    - Apply consistently to shared styles and feature screens.
- [ ] Fix layout/overflow issues for narrow terminal widths.
  - Completion criteria:
    - Verify key flows at typical terminal widths (e.g., 80, 100, 120 columns).
    - No critical clipping/overlap in primary user flows.
- [ ] Improve information architecture of key workflows.
  - Completion criteria:
    - Transfer / aggregate / lock / metadata screens have clear primary action, secondary actions, and confirmation/error presentation.
    - Reduce cognitive load in forms (grouping, labels, defaults, inline validation messaging).
- [ ] Add regression tests for UI-critical behavior.
  - Completion criteria:
    - Add/update Textual tests for core interaction and layout-sensitive behavior where possible.
    - Ensure tests run in CI path without live-key dependency.
- [ ] Final enterprise UX acceptance pass.
  - Completion criteria:
    - Conduct end-to-end walkthrough using realistic operator tasks.
    - Record acceptance notes and remaining minor polish items in memory log.

## Notes
- `/tmp/symbol-quick-wallet-test-key` was generated on 2026-03-05 (address: `TA73WKJLYQ4D54YTWEEBQZP53GECALKIAXXRQCA`) for strict run reproducibility; account is currently unfunded.

## Entry: Key Stability Investigation (2026-03-05)
- Decision/Intent:
  - Investigate why live test key address changed and prevent unintended key rotation in this environment.
- Findings:
  - Pytest live fixtures do not rewrite the key file.
  - Key changes are caused by `scripts/setup_test_key.py` regeneration flow (especially when `--force` is used).
  - `/tmp` key paths are volatile and not suitable for stable long-lived local test identity.
  - Re-run with funded legacy key (address `TBXMLQGINECK33QR4SQJOACJB7NS7NZS2IGUILY`) confirmed non-balance bonded visibility failures:
    - `tests/features/aggregate/test_integration.py:620`
    - `tests/features/aggregate/test_integration.py:712`
- Implemented safeguards:
  - `scripts/setup_test_key.py`
    - writes `<key-file>.address` sidecar
    - `--force` now requires `--confirm-current-address <CURRENT_ADDRESS>`
    - warns when target path is under `/tmp`
    - handles invalid/empty existing key files safely
  - `tests/conftest.py`
    - added `--expected-test-address` and `SYMBOL_TEST_EXPECTED_ADDRESS`
    - validates loaded live key address against expected address
    - validates against `<test-key-file>.address` sidecar when present
  - `tests/README.md`
    - documented key immutability guard and expected-address usage
