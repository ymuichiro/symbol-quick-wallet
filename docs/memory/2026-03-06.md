# 2026-03-06

## Entry 1
- Time: 2026-03-06 JST
- Decision/Intent: Prevent accidental live test key rotation in this environment and keep a stable funded account for on-chain tests.
- Action:
  - verified `/tmp/symbol-quick-wallet-*.key` files were absent on this date
  - confirmed reusable funded test account address `TBXMLQGINECK33QR4SQJOACJB7NS7NZS2IGUILY` still has balance on testnet
  - fixed local key path to persistent location:
    - `/Users/you/.config/symbol-quick-wallet/keys/testnet_live.key`
    - `/Users/you/.config/symbol-quick-wallet/keys/testnet_live.key.address`
  - added key immutability memo to `AGENTS.md`:
    - funded key must not be overwritten/regenerated
    - avoid `/tmp` for stable local runs
    - use expected-address guard in pytest when needed
- Result:
  - stable local key path established for future live runs
  - operational memo recorded in repository guidance
- Next:
  - execute strict live runs using the persistent key path and expected-address guard.

## Entry 2
- Time: 2026-03-06 JST
- Decision/Intent: Clarify account-state continuity policy so funded test-account balance is not effectively reset by key regeneration.
- Action:
  - extended `AGENTS.md` live-key policy with explicit note that changing a funded test key breaks balance continuity
  - re-verified current fixed test account address from persistent key metadata:
    - `TBXMLQGINECK33QR4SQJOACJB7NS7NZS2IGUILY`
  - checked XYM amount via two testnet nodes (`sym-test-01`, `sym-test-03`) and confirmed matching value (`17232800` micro XYM)
- Result:
  - policy and current account state are aligned for next funding/test cycle
  - no key rotation observed during this check
- Next:
  - continue live tests with `--test-key-file /Users/you/.config/symbol-quick-wallet/keys/testnet_live.key` and expected-address guard when needed.

## Entry 3
- Time: 2026-03-06 JST
- Decision/Intent: Re-validate full live integration coverage after user top-up using fixed key and expected-address guard.
- Action:
  - ran: `SYMBOL_TEST_RUN_LIVE=1 uv run pytest -q -c pyproject.toml tests --test-key-file /Users/you/.config/symbol-quick-wallet/keys/testnet_live.key --require-live-key --expected-test-address TBXMLQGINECK33QR4SQJOACJB7NS7NZS2IGUILY -m "integration and not slow" -rs`
  - result: `96 passed, 421 deselected`
  - ran: `SYMBOL_TEST_RUN_LIVE=1 uv run pytest -q -c pyproject.toml tests --test-key-file /Users/you/.config/symbol-quick-wallet/keys/testnet_live.key --require-live-key --expected-test-address TBXMLQGINECK33QR4SQJOACJB7NS7NZS2IGUILY -m "integration and slow" -rs`
  - result: `4 failed, 22 passed, 491 deselected`
  - failing tests:
    - `tests/features/aggregate/test_integration.py::TestAggregateBondedLiveTransaction::test_live_aggregate_bonded_full_workflow`
    - `tests/features/aggregate/test_integration.py::TestAggregateBondedLiveTransaction::test_live_aggregate_bonded_reaches_partial_or_confirmed`
    - `tests/features/mosaic/test_integration.py::TestMosaicLiveTransaction::test_live_create_mosaic_with_divisibility`
    - `tests/features/mosaic/test_integration.py::TestMosaicLiveTransaction::test_live_create_and_verify_mosaic`
  - post-run XYM check on `sym-test-01` and `sym-test-03`: `36863300` micro XYM (both nodes)
- Result:
  - live integration suite is not fully green in current network/account state.
- Next:
  - handle aggregate bonded observer-visibility failures (capability-dependent handling)
  - adjust mosaic live-test minimum balance guard or funding strategy to avoid announce-time insufficient-balance failures.

## Entry 4
- Time: 2026-03-06 JST
- Decision/Intent: Separate unit-test health from live slow-integration failures.
- Action:
  - ran: `uv run pytest -q -c pyproject.toml tests -m unit -rs`
  - result: `114 passed, 403 deselected`
- Result:
  - unit tests are green; remaining failures are isolated to slow live integration scenarios.
- Next:
  - prioritize aggregate bonded observer visibility handling and mosaic live-test funding guard.

## Entry 5
- Time: 2026-03-06 JST
- Decision/Intent: Implement live slow test stabilization plan while keeping aggregate observer visibility as fail (not skip).
- Action:
  - added shared live balance helper fixture `ensure_live_min_balance` in `tests/conftest.py`
  - applied helper-based min-balance checks across slow live tests (aggregate/mosaic/lock/multisig/transfer/metadata)
  - enhanced aggregate observer visibility probing in `tests/features/aggregate/test_integration.py`:
    - polling timeout/poll interval via env vars:
      - `SYMBOL_TEST_OBSERVER_VISIBILITY_TIMEOUT` (default 180)
      - `SYMBOL_TEST_OBSERVER_VISIBILITY_POLL` (default 10)
    - added per-node diagnostic collection (`transactionStatus`, confirmed lookup, partial lookup, endpoint errors)
    - preserved fail behavior and embedded diagnostics in assertion message when visibility is unavailable
  - updated mosaic live tests in `tests/features/mosaic/test_integration.py`:
    - introduced `SYMBOL_TEST_MOSAIC_MIN_BALANCE_MICRO` (default 50000000)
    - converted announce-time `Failure_Core_Insufficient_Balance` into explicit skip with balance context
  - updated `tests/README.md` with new env knobs, strict-run example, and 50+ XYM guidance
- Validation:
  - `uv run ruff check .` -> pass
  - `uv run ty check src/` -> pass
  - targeted 4 tests (2 aggregate bonded + 2 mosaic live): `2 failed, 2 skipped`
    - aggregate failures now include detailed observer diagnostics
    - mosaic tests skip by min-balance gate (expected)
  - regression `integration and not slow`: `96 passed, 421 deselected`
  - regression `unit`: `114 passed, 403 deselected`
  - regression `integration and slow`: `1 failed, 20 passed, 5 skipped, 491 deselected`
    - remaining fail: aggregate bonded full workflow observer visibility unavailable (with diagnostics)
- Result:
  - reduced slow live failures from prior 4 to 1 in current environment
  - remaining failure is an intentional fail-policy assertion with richer diagnostics.
- Next:
  - if full green is required under this environment, resolve observer-node visibility gap or relax aggregate fail policy (currently set to fail by design).
