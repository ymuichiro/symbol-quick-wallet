## 2026-03-07

### Decision / Intent
- Resolve aggregate bonded live instability by ensuring hash-lock references the signed aggregate hash.
- Keep observer-visibility assertion as fail semantics, and keep low-balance behavior as skip semantics for live slow runs.
- Validate current branch state with strict live suites using persistent key + expected-address guard.

### Implementation Notes
- Updated `src/features/aggregate/service.py`:
  - Added `calculate_transaction_hash_from_signed_payload`.
  - Updated `create_hash_lock(..., aggregate_hash: str | None = None)` to allow explicit aggregate hash.
  - Changed aggregate bonded workflow to sign aggregate first, derive hash from signed payload, and create hash lock with that hash.
- Updated `tests/conftest.py`:
  - Added spendable XYM estimation helper that subtracts active hash locks from total balance.
  - `ensure_live_min_balance` now gates by spendable amount and reports `total/active_locked/spendable`.
- Updated `tests/features/aggregate/test_service.py`:
  - Added unit test for explicit hash usage in hash lock.
  - Added unit test validating payload-derived hash calculation.

### Verification Executed
- `uv run ruff check src/features/aggregate/service.py tests/conftest.py tests/features/aggregate/test_service.py`
  - pass
- `uv run pytest -q -c pyproject.toml tests/features/aggregate/test_service.py -m "not slow" -rs`
  - `40 passed, 7 deselected`
- `SYMBOL_TEST_RUN_LIVE=1 uv run pytest -q -c pyproject.toml tests --test-key-file /Users/you/.config/symbol-quick-wallet/keys/testnet_live.key --require-live-key --expected-test-address TBXMLQGINECK33QR4SQJOACJB7NS7NZS2IGUILY -m "integration and not slow" -rs`
  - `96 passed, 423 deselected`
- `SYMBOL_TEST_RUN_LIVE=1 uv run pytest -q -c pyproject.toml tests --test-key-file /Users/you/.config/symbol-quick-wallet/keys/testnet_live.key --require-live-key --expected-test-address TBXMLQGINECK33QR4SQJOACJB7NS7NZS2IGUILY -m "integration and slow" -rs`
  - `20 passed, 6 skipped, 493 deselected`
  - skips were all balance-gate based (`Insufficient balance ... need 11 XYM/10.5 XYM/50 XYM`), no test failures
- `uv run pytest -q -c pyproject.toml tests -m unit -rs`
  - `114 passed, 405 deselected`
- `uv run pytest -q -c pyproject.toml tests -rs`
  - `460 passed, 59 skipped` (default run without strict live key args)

### Current State
- No failing tests in executed suites.
- Strict live slow suite behavior is now deterministic: pass where balance is sufficient, skip where threshold is not met.

### Handover Preparation
- Added repository-root handover document: `HANDOVER.md`.
- Documented:
  - implemented fixes,
  - exact verification commands/results,
  - remaining tasks for next engineer (mainly funding-dependent slow live pass conversion and UI polish continuation).
